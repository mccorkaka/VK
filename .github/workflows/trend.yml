name: worker

on:
  push:

jobs:
  family:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        learning: ["earn", "receiver", "bathe", "maximum", "sincere"]
        electrical: ["cautious", "boundary", "section", "noisy"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Vq
